package net.minecraft.src.buildcraft.core;

import java.util.TreeMap;

public class Agent {
	
	private TreeMap <BlockIndex, Agent> chunks = new TreeMap <BlockIndex, Agent> ();
	
	public Agent getAgent (int x, int y, int z) {
		return agents.get(new BlockIndex(i, j, k));
		
	}
}
