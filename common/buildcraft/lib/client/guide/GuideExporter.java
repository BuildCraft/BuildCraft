package buildcraft.lib.client.guide;

import java.io.File;

public class GuideExporter {
    public static void export(File folder) {
        for (GuideManager manager : GuideManager.managers.values()) {
//            manager.
        }
    }
}
