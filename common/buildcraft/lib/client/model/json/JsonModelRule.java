package buildcraft.lib.client.model.json;

/**
 * A rule for changing a model's elements. The most basic example is rotating an entire model based of a single property.
 */
public class JsonModelRule {
    
}
